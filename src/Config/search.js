export const DEFAULT_PAGE = 0;

export const DEFAULT_PAGE_SIZE = {
  orders: 5,
  wishlist: 5,
  addresses: 6,
  files: 10,
};

export const DEFAULT_SORT = {
  orders: 'createdAt,desc',
  wishlist: 'addedAt,desc',
  addresses: 'createdAt,desc',
  files: 'createdAt,desc',
};
